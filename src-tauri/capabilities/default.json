{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — only core permissions, everything else stays local to the Node server.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
